import styled from 'styled-components';

const Main = styled.main`
  display: flex;
  margin-top: 2.5rem;
  justify-content: center;
`;

const Wrapper = styled.div`
  width: 600px;
  box-shadow: 0 10px 50px -3px gray, 0 10px 6px -2px gray;
  border-radius: 10px;
  padding: 2.5rem;
`;

export { Main, Wrapper };
